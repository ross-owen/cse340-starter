<!--show the title if exists, otherwise redirect back to the homepage-->
<% if (title) { %>
    <h1><%= title %></h1>
<% } else {
    res.redirect('/')
} %>

<!--show the messages later when we're ready-->
<%# messages() %>

<!--show the vehicle detail-->
<%- detail %>